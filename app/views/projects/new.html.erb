<%= form_for @project do |f| %>
   <div class="control-group">
     <%= f.label :name %>
     <div class="controls">
       <%= f.text_field :name %>
     </div>
   </div>
   <div class="form-actions">
     <%= f.submit nil %>
     <%= link_to 'Cancel', projects_path %>
   </div>
 <% end %>